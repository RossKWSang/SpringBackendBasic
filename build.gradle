plugins {
	id 'java'
	id 'org.springframework.boot' version '2.7.9'
	id 'io.spring.dependency-management' version '1.0.15.RELEASE'
}

allprojects {
	repositories {
		mavenCentral()
	}
}

bootJar { // 해당 패키지는 bootJar로 패키징하지 않겠다
	enabled = false
}

jar { // 해당 패키지는 jar로 패키징하지 않겠다
	enabled = false
}
